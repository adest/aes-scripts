# ─── TYPE DEFINITIONS ────────────────────────────────────────────────────────

types:
  greeter:
    params:
      name: ~          # required (~ = no default)
      greeting: hello  # optional, defaults to "hello"
    command: echo {{ .greeting }} {{ .name }}
